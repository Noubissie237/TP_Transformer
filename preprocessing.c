#include "preprocessing.h"
#include <stdio.h>
#include <stdlib.h>

PreprocessedData preprocess(Dataset dataset) {
    PreprocessedData pdata;
    // Code pour prétraiter les données
    return pdata;
}

void free_preprocessed_data(PreprocessedData pdata) {
    // Libération de la mémoire du PreprocessedData
}
