#ifndef TRANSFORMER_H
#define TRANSFORMER_H

typedef struct {
    // Définir la structure du Transformer
} Transformer;

Transformer init_transformer();
void free_transformer(Transformer transformer);

#endif // TRANSFORMER_H
